# Google Maps API Key Configuration
# Copy this file to .env and replace the placeholder value with your actual API key

# Google Maps API Key (REQUIRED)
GoogleMaps__ApiKey=YOUR_GOOGLE_MAPS_API_KEY_HERE

# NOTE: Connection string and JWT settings are now in appsettings.json
# Only the Google Maps API key needs to be in .env for security
