# This file ensures the uploads directory is tracked by Git
# All uploaded files (PDFs, images) are ignored via .gitignore,
# but this directory structure is preserved for the application to work properly.
